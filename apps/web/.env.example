NEXT_PUBLIC_API_URL=http://localhost:5000

# GitHub App slug (from the App's public URL: github.com/apps/<slug>).
# Used to build the install link: github.com/apps/<slug>/installations/new
NEXT_PUBLIC_GITHUB_APP_NAME=your-github-app-slug
